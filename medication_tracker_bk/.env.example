# 本地开发用的 JWT secret fallback 会用 'dev-only-secret-change-in-prod'。
# 生产环境务必通过 systemd EnvironmentFile 或 CI/CD 注入真 secret。
JWT_SECRET_KEY=

# 可选：覆盖默认 CORS 白名单（逗号分隔）
# CORS_ORIGINS=https://lyssom.tech,https://metadao.lyssom.tech
